name: Run e2e test
on:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
   
    strategy:
      matrix:
        node-version: [ 18.18.2 ]

    steps:
      - name: check if PR title follows conventional commits specs
        uses: amannn/action-semantic-pull-request@v3.4.2
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_TOKEN }}
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies
        run: npm install --legacy-peer-deps  
      - name: Build
        run: npm run build --if-present
      - name: Lint
        run: npm run lint
      - name: Unit test
        run: npm run test
      - name: Output items in current directory
        run: ls
      # - name: Copy .env file
      #   run: cp .env.example .env
      # - name: Import schema.sql
      #   run: mysql --host=127.0.0.1 --user=nkwa --password=nkwapassword -D oyesaving < test/setup/db/schema.sql
      # - name: Run migrations
      #   run: npm run migrate:up
      # - name: Set mobile money access control defaults
      #   run: mysql --host=127.0.0.1 --user=nkwa --password=nkwapassword -D oyesaving < test/setup/db/momo_access_control_defaults.sql
      # - name: Seed data
      #   run: npm run data:seed
      # - name: E2E tests
      #   run: npm run test:e2e
